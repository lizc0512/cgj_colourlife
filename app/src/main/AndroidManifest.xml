<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tg.coloursteward">

    <permission
        android:name="com.tg.coloursteward.permission.JPUSH_MESSAGE"
        android:protectionLevel="signature" />

    <!-- Required -->
    <uses-permission android:name="com.tg.coloursteward.permission.JPUSH_MESSAGE" />
    <uses-permission android:name="android.permission.RECEIVE_USER_PRESENT" />

    <!-- Optional. Required for location feature -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /> <!-- 用于开启 debug 版本的应用在6.0 系统上 层叠窗口权限 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- 用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 用于写入缓存数据到扩展存储卡 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 用于申请调用A-GPS模块 -->
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <!-- 用于申请获取蓝牙信息进行室内定位 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:name=".application.CityPropertyApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity1"
            android:screenOrientation="portrait"
            android:theme="@style/BaseAppTheme"
            android:windowSoftInputMode="stateUnspecified|adjustPan">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>

        <activity
            android:name="com.youmai.hxsdk.activity.IMConnectionActivity"
            android:configChanges="keyboardHidden|orientation"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/HxSdkNewTheme"
            android:windowSoftInputMode="adjustResize|stateHidden">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>


        <activity
            android:name="com.youmai.hxsdk.activity.IMGroupActivity"
            android:configChanges="keyboardHidden|orientation"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/HxSdkNewTheme"
            android:windowSoftInputMode="adjustResize|stateHidden">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>


        <activity
            android:name=".MainActivity"
            android:configChanges="locale|orientation|keyboard|keyboardHidden"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />
        <!-- 弹出提现新消息页面 -->
        <activity android:name=".AlertActivity" />
        <!-- 投诉页面 -->
        <activity android:name=".ComplaintActivity" />
        <!-- 消息推送消息列表 -->
        <activity android:name=".DeskTopActivity" />
        <!-- 扫描二维码页面 -->
        <activity android:name=".MipcaActivityCapture" />
        <!-- 公共报修 -->
        <activity android:name=".RepairsPulbicActivity" />
        <!-- 个人中心 -->
        <activity android:name=".UserInfoActivity" />
        <!-- 图片显示 -->
        <activity android:name=".ShowImageActivity" />
        <!-- 更多设置 -->
        <activity android:name=".SettingActivity" />
        <!-- 关于app -->
        <activity android:name=".AboutUsActivity" />
        <!-- 修改密码 -->
        <activity android:name=".ModifiedPasswordActivity" />
        <!-- 意见反馈 -->
        <activity android:name=".OpinionActivity" />
        <!-- 获取手机通讯录 -->
        <activity android:name=".ContactsActivity" />
        <!-- 员工名片 -->
        <activity android:name=".EmployeeDataActivity" />
        <activity android:name="com.colorfullife_verify.Oauth_LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- 通讯录搜索 -->
        <activity
            android:name=".HomeContactSearchActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name" />
        <!-- 我的饭票 -->
        <activity
            android:name=".RedpacketsBonusMainActivity"
            android:label="@string/app_name" />
        <!-- 浏览器BaseActivity -->
        <activity
            android:name=".MyBrowserActivity"
            android:configChanges="locale|orientation|keyboard|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <!-- 获取临时密码 -->
        <activity
            android:name=".TemporaryPassWordActivity"
            android:label="@string/app_name" />
        <!-- 邀请同事 -->
        <activity
            android:name=".InviteRegisterActivity"
            android:label="@string/app_name" />
        <!-- 组织框架 -->
        <activity
            android:name=".HomeContactOrgActivity"
            android:label="@string/app_name" />
        <!-- 战略地图 -->
        <activity
            android:name=".MapDetailActivity"
            android:configChanges="locale|orientation|keyboard|keyboardHidden"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="stateHidden" />
        <!-- 我的下载 -->
        <activity
            android:name=".DownloadManagerActivity"
            android:label="@string/app_name" />
        <!-- 集体奖惩明细 -->
        <activity
            android:name=".BonusRecordActivity"
            android:label="@string/app_name" />
        <!-- 奖金包(个人) -->
        <activity
            android:name=".BonusRecordPersonalActivity"
            android:label="@string/app_name" />
        <!-- 详情(奖金包) -->
        <activity
            android:name=".BonusRecordDetailNewActivity"
            android:label="@string/app_name" />
        <!-- 详情(绩效) -->
        <activity
            android:name=".EffectDetailActivity"
            android:label="@string/app_name" />
        <!-- 饭票收支明细 -->
        <activity
            android:name=".RedpacketsRecordActivity"
            android:label="@string/app_name" />
        <!-- 我的饭票(提现与转账) -->
        <activity
            android:name=".RedpacketsMainActivity"
            android:label="@string/app_name" />
        <!-- 给同事发饭票 -->
        <activity
            android:name=".RedpacketsShareMainActivity"
            android:label="@string/app_name" />
        <!-- 给同事发饭票 -->
        <activity
            android:name=".RedpacketsTransferMainActivity"
            android:label="@string/app_name" />
        <!-- 通讯录同事 -->
        <activity
            android:name=".RedpacketsContactsActivity"
            android:label="@string/app_name" />
        <!-- 绑定彩之云账号 -->
        <activity
            android:name=".RedpacketsBindCZYMainActivity"
            android:label="@string/app_name" />
        <!-- 选择银行卡 -->
        <activity
            android:name=".RedpacketsBankCardListActivity"
            android:label="@string/app_name" />
        <!-- 绑定银行卡 -->
        <activity
            android:name=".RedpacketsBindBankCardActivity"
            android:label="@string/app_name" />
        <!-- 提现完成页面 -->
        <activity
            android:name=".RedpacketsWithdrawFinishedActivity"
            android:label="@string/app_name" />
        <!-- 提现 -->
        <activity
            android:name=".RedpacketsWithdrawMainActivity"
            android:label="@string/app_name" />
        <!-- 提现记录 -->
        <activity
            android:name=".RedpacketsWithdrawRecordActivity"
            android:label="@string/app_name" />
        <!-- 绑定彩之云 -->
        <activity
            android:name=".RedpacketsBindCZYConfirmActivity"
            android:label="@string/app_name" />
        <!-- 提现明细 -->
        <activity
            android:name=".RedpacketsWithdrawRecordDetailActivity"
            android:label="@string/app_name" />
        <!-- 开门 -->
        <activity
            android:name=".DoorActivity"
            android:exported="true"
            android:label="@string/app_name" />
        <!-- 申请手机开门 -->
        <activity
            android:name=".DoorApplyDredgeActivity"
            android:label="@string/app_name" />
        <!-- 申请门禁 -->
        <activity
            android:name=".DoorApplyActivity"
            android:label="@string/app_name" />
        <!-- 门禁授权 -->
        <activity
            android:name=".DoorAuthorizationActivity"
            android:label="@string/app_name" />
        <!-- 编辑门禁 -->
        <activity
            android:name=".DoorEditActivity"
            android:label="@string/app_name" />
        <!-- 开门结果处理 -->
        <activity
            android:name=".DoorOpenActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ScanResultDoorOpenActivity"
            android:label="@string/app_name" />
        <!-- 日志 -->
        <activity
            android:name=".SettingPolicyActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ScanResultActivity"
            android:label="@string/app_name" />
        <!-- 扫描结果 -->
        <activity
            android:name=".WaterBoxTypeActivity"
            android:label="@string/app_name" />
        <!-- 申请 -->
        <activity
            android:name=".DoorApplyDetailActivity"
            android:label="@string/app_name" />
        <!-- 授权 -->
        <activity
            android:name=".DoorAuthorizationApproveActivity"
            android:label="@string/app_name" />
        <!-- 授权详情 -->
        <activity
            android:name=".DoorControlAuthorizationDetailActivity2"
            android:label="@string/app_name" />
        <!-- 数据看板 -->
        <activity
            android:name=".DataShowActivity"
            android:label="@string/app_name" />
        <!-- 即时分成 -->
        <activity android:name=".AccountActivity" />
        <!-- 即时分成明细 -->
        <activity android:name=".AccountDetailActivity" />

        <!--搜索-->
        <activity
            android:name=".module.search.GlobalSearchActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />

        <service android:name=".serice.MessageService" />

        <!--分享-->
        <activity
            android:name="com.mob.tools.MobUIShell"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:windowSoftInputMode="stateHidden|adjustResize">

            <!-- 调用新浪原生SDK，需要注册的回调activity -->
            <intent-filter>
                <action android:name="com.sina.weibo.sdk.action.ACTION_SDK_REQ_ACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!--集成line客户端登录授权，需要添如下格式的过滤器-->
            <intent-filter>
                <data android:scheme="line.1477692153" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- ShareSDK3.1.0版本之前才需要在MobUIShell下面注册此意图，3.1.0版本之后（包含3.1.0）不需要注册此intent-->
            <intent-filter>
                <data android:scheme="tencent100371282" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <!-- 填写您从Mob开发者后台中得到的Appkey和AppSecret -->
        <meta-data
            android:name="Mob-AppKey"
            android:value="215979e18d853" />
        <meta-data
            android:name="Mob-AppSecret"
            android:value="0e45336a2e68ead59bb90df01be1c619" />


        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.tg.coloursteward.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"
                tools:replace="android:resource" />
        </provider>
        <!-- 极光推送 -->
        <!-- Rich push 核心功能 since 2.0.6 -->
        <activity
            android:name="cn.jpush.android.ui.PopWinActivity"
            android:exported="false"
            android:theme="@style/MyDialogStyle" />

        <!-- Required SDK核心功能 -->
        <activity
            android:name="cn.jpush.android.ui.PushActivity"
            android:configChanges="orientation|keyboardHidden"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="cn.jpush.android.ui.PushActivity" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="com.tg.coloursteward" />
            </intent-filter>
        </activity>
        <!-- Required  SDK核心功能 -->
        <service
            android:name="cn.jpush.android.service.DownloadService"
            android:enabled="true"
            android:exported="false" />

        <!-- Required SDK 核心功能 -->
        <!-- 可配置android:process参数将PushService放在其他进程中 -->
        <service
            android:name="cn.jpush.android.service.PushService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTER" />
                <action android:name="cn.jpush.android.intent.REPORT" />
                <action android:name="cn.jpush.android.intent.PushService" />
                <action android:name="cn.jpush.android.intent.PUSH_TIME" />
            </intent-filter>
        </service>

        <!-- since 1.8.0 option 可选项。用于同一设备中不同应用的JPush服务相互拉起的功能。 -->
        <!-- 若不启用该功能可删除该组件，将不拉起其他应用也不能被其他应用拉起 -->
        <service
            android:name="cn.jpush.android.service.DaemonService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.DaemonService" />

                <category android:name="com.tg.coloursteward" />
            </intent-filter>
        </service>

        <!-- Required SDK核心功能 -->
        <receiver
            android:name="cn.jpush.android.service.PushReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED_PROXY" />
                <!-- Required  显示通知栏 -->
                <category android:name="com.tg.coloursteward" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
            <!-- Optional -->
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Required SDK核心功能 -->
        <receiver
            android:name="cn.jpush.android.service.AlarmReceiver"
            android:exported="false" />

        <!-- User defined.  For test only  用户自定义的广播接收器 -->
        <receiver
            android:name=".push.MyReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTRATION" />
                <!-- Required  用户注册SDK的intent -->
                <action android:name="cn.jpush.android.intent.MESSAGE_RECEIVED" />
                <!-- Required  用户接收SDK消息的intent -->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED" />
                <!-- Required  用户接收SDK通知栏信息的intent -->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_OPENED" />
                <!-- Required  用户打开自定义通知栏的intent -->
                <action android:name="cn.jpush.android.intent.ACTION_RICHPUSH_CALLBACK" />
                <!-- Optional 用户接受Rich Push Javascript 回调函数的intent -->
                <action android:name="cn.jpush.android.intent.CONNECTION" />
                <!-- 接收网络变化 连接/断开 since 1.6.3 -->
                <category android:name="com.tg.coloursteward" />
            </intent-filter>
        </receiver>

        <!-- Required  . Enable it you can get statistics data with channel -->
        <meta-data
            android:name="JPUSH_CHANNEL"
            android:value="developer-default" />
        <meta-data
            android:name="JPUSH_APPKEY"
            android:value="a923c16ad07f805480d67b2b" />

        <!-- 更新App服务 -->
        <service android:name=".serice.DownLoadService" />

        <activity android:name=".RedpacketsRecordDetailActivity" />
        <activity android:name=".SearchContactListActivity" />
        <activity android:name=".SearchHomeListActivity" />
        <activity android:name=".SearchManageMentListActivity" />
        <activity android:name=".BranchActivity" />
        <activity android:name=".PublicAccountActivity" />
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".ForgetPasswordVerifyActivity" />

        <activity android:name=".module.groupchat.GroupListActivity" />

        <activity android:name=".module.groupchat.AddContactsCreateGroupActivity" />
    </application>

</manifest>